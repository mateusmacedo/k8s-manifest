apiVersion: v1
kind: ServiceAccount
metadata:
  name: redis
  namespace: "default"
  labels:
    app.kubernetes.io/name: redis
    app.kubernetes.io/instance: redis
secrets:
  - name: redis
automountServiceAccountToken: true
